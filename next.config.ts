import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // 注意: Next.js 16ではinstrumentation.tsがデフォルトで有効なため、
  // experimental.instrumentationHookは不要です
};

export default nextConfig;
